import React from "react";
import EmptyState from "../../components/EmptyState.jsx";

export default function System() {
    return (
        <EmptyState
            title="System"
            subtitle="Coming next: CPU/GPU metrics, event rate, camera status, detector latency."
        />
    );
}
