import React from "react";
import EmptyState from "../../components/EmptyState.jsx";

export default function Config() {
    return (
        <EmptyState
            title="Config"
            subtitle="Coming next: YAML editor + validation + apply. For now keep backend config static."
        />
    );
}
